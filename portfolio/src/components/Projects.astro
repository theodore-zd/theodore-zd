---
import { resume } from "@/data/resume"
import CardContent from "./CardContent.astro"
import TechBadge from "./TechBadge.astro"
import TerminalPrompt from "./TerminalPrompt.astro"
import GithubCard from "./GithubCard.astro"
import { slugFromTitle } from "@/lib/slug"
import SectionLabel from "./SectionLabel.astro"
import BasicCard from "./BasicCard.astro"


const delayClass = (i: number) => `delay-${((i % 3) + 1) * 100}`
const projects = resume.projects
const githubProjects = resume.githubProjects
---

<section id="projects" class="border-b border-border py-24">
    <div class="mx-auto max-w-6xl px-6">
        <SectionLabel label="projects" />

        <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
            {githubProjects.map((project, index) => (
                <div class:list={["h-full animate-fade-up", delayClass(index)]}>
                    <GithubCard project={project} />
                </div>
            ))}
            {projects.map((project, index) => (
                <a href={`/projects/${slugFromTitle(project.title)}`} class="block h-full hover:no-underline">
                    <BasicCard index={index}>
                        <CardContent class="flex h-full flex-col gap-4 p-6">
                            <div class="flex flex-col gap-2">
                                <div class="flex items-center gap-2">
                                    <span class="flex size-6 shrink-0 items-center justify-center rounded bg-primary/10">
                                        <TerminalPrompt />
                                    </span>
                                    <h3 class="text-base font-semibold">
                                        {project.title}
                                    </h3>
                                </div>
                                <p class="text-sm text-muted-foreground line-clamp-3">
                                    {project.description}
                                </p>
                            </div>

                            <div class="flex flex-wrap gap-1.5 mt-auto">
                                {project.tools.map((tool) => (
                                    <TechBadge tech={tool} />
                                ))}
                            </div>
                        </CardContent>
                    </BasicCard>
                </a>
            ))}
        </div>
    </div>
</section>
